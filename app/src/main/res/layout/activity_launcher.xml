<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0099cc"
    android:orientation="vertical"
    tools:context=".LauncherActivity"
    >

  <!-- The primary full-screen view. This can be replaced with whatever view
       is needed to present your content, e.g. VideoView, SurfaceView,
       TextureView, etc. -->
  <ImageView
      android:layout_width="200dp"
      android:layout_height="200dp"
      android:layout_gravity="center_horizontal|top"
      android:layout_marginTop="50dp"
      android:background="@drawable/extension_qsds"
      />

  <!-- This FrameLayout insets its children based on system windows using
       android:fitsSystemWindows. -->
  <TextView
      android:id="@+id/textView"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center_horizontal"
      android:layout_marginBottom="8dp"
      android:layout_marginTop="8dp"
      android:text="หม่อนไหมยุคดิจิตอล"
      android:textColor="#ffffff"
      android:textSize="@dimen/title_text_size"
      />

  <GridLayout
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center"
      android:layout_marginTop="12dp"
      android:columnCount="2"
      android:rowCount="2"
      >

    <Button
        android:id="@+id/sell"
        android:text="ตลาดขาย"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
        style="@style/menu_button"
        android:textSize="@dimen/button_text_size"
        />

    <Button
        android:id="@+id/buy"
        android:text="ตลาดซื้อ"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
        style="@style/menu_button"
        android:textSize="@dimen/button_text_size"
        />

    <Button
        android:id="@+id/knowledge"
        android:text="วีดิทัศน์"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
        style="@style/menu_button"
        android:textSize="@dimen/button_text_size"
        />

    <Button
        android:id="@+id/qsds_link"
        android:text="กรมหม่อนไหม"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
        style="@style/menu_button"
        android:textSize="@dimen/button_text_size"
        />
  </GridLayout>

  <ImageView
      android:id="@+id/imageView"
      android:layout_width="400dp"
      android:layout_height="200dp"
      android:layout_gravity="right|bottom"
      android:background="@drawable/worm"
      android:scaleType="fitStart"
      />

</LinearLayout>
